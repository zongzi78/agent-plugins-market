# 阶段 2：架构梳理

**目标**：还原系统架构。

> **本 SOP 的上下文**：
> - 文件角色定义 → `../SKILL.md`「2. 文件角色对照表」
> - 质量规则（正向/负向） → `../SKILL.md`「1. 通用规则」
> - 产出模板路径见 SKILL.md「各阶段资源映射」表

## 执行步骤

1. **分析模块依赖**：扫描 import/include 语句、包结构、模块调用链，识别模块间依赖关系。

   **模块发现启发式**（从代码仓库提取候选模块时的优先级）：
   - 以一级源码目录作为初始候选模块
   - 对于 utils、common、shared 等命名模糊的目录，通过依赖分析将其拆解或归并到明确职责的模块中
   - 识别横切关注点（如日志、鉴权、配置、监控、异常处理），将其作为独立模块抽取，不强行归属业务模块
   - 若存在包/命名空间，可将公开 API 聚集的包视为一个候选模块

2. **识别数据流向**：追踪关键业务流程的调用链路。
   **L1/L2边界规则（强制）**：
   - L1（本文档 00-架构.md）：以模块为粒度描述跨模块数据传递，
     每个步骤 = "模块A → 模块B，传递什么数据、什么形态变化"
   - L2（03-详细设计）：模块内部的函数调用链、条件分支、循环逻辑
   - 判断标准：描述的是"模块内部如何实现"（如"创建XX实例""取最后N个采样点平均值"）
     → 属于L2的部分，不应出现在此处
   - 每条数据流必须涉及多个不同模块（跨模块交互）。单个模块内部的操作链不属于数据流——归入 L2（03-详细设计）。
   - **数据流数量**：由项目的端到端业务场景自然决定，不设最少/最多限制。

   **排查角度**：从以下角度逐一排查数据流，仅描述代码中确实存在的路径：
   - 系统启动流程
   - 每条写路径（用户操作 → 数据持久化）
   - 每条读路径（用户请求 → 数据展示）
   - 每条系统间交互路径（外部触发 → 系统响应）
   - 每条定时/后台任务路径

3. **识别外部依赖**：从配置文件、连接代码中提取外部系统和中间件

4. **绘制依赖图**：用 Mermaid 绘制模块依赖图
   - 每个模块必须是独立的节点
   - 按层级分组（表示层 / 业务逻辑层 / 数据访问层 / 基础设施层）
   - 箭头方向表示依赖关系（A -> B 表示 A 依赖 B）
   - 如存在循环依赖，如实画出——循环依赖条目写入观察便签
   - 如果系统比较复杂，或者模块较多，可以分为多个 Mermaid 图进行展示，以保证模块间关系的清晰表述。

5. **记录理解型观察**：在梳理架构过程中，记录以下仅靠 grep 无法发现的观察：
   - 单类 ≥3 种职责的模块（由 diagnose 范畴5.1 在阶段6统一检测，此处仅记录一句话事实）
   - 循环依赖（需跨文件追踪调用链确认的）
   - 其他需要语义理解的架构级发现
   → 便签格式见 project-log.md 模板「诊断观察便签」区段

6. **填充认知边界地图**（三子表）：
   - 已知已知表：通过阅读具体代码确认的事实（每条带符号锚点证据，如 ClassName::methodName() 或 filename.cpp -> functionName()）
   - 已知未知表：AI 无法确定、需要人工补充的内容（写具体问题）
   - 推断结论表：从代码模式推断但未确认的设计决策

   **验证标准**：
   - 已知已知：证据列必须使用符号锚点格式（ClassName::methodName() 或 filename.cpp → functionName()）
   - 已知未知：必须写具体问题（如"身份验证机制：未找到 token 生成/验证的代码位置"），不要写模糊描述
   - 推断结论的可靠性：有符号锚点 = 代码证据充分；无锚点 = 基于有限证据，待确认

### 写入观察便签

→ 格式与规则见 project-log.md 模板「诊断观察便签」区段（唯一权威源）。

示例：
```markdown
### 阶段2 观察
- [AV] [mainWindow.h → CHarmonicMainWindow] 单类承载 5 种职责（UI编排/扫描管理/模型生成/仿真执行/工程日志），40+ 成员变量
- [DD] [simtaskstruct.h → SimTask::timeout] 硬编码 timeout(3000)，无常量定义，无配置入口
```

## 产出

- `.ai/doc/00-架构.md`（基于模板生成）
- 观察便签 -> `.ai/project-log.md`（临时，阶段 6 消费）

## 产出后自检

**Agent 机械自检**（生成完成后立即逐项核对）：
- [ ] 所有模块已列出，每个含职责描述和复杂度概要
- [ ] Mermaid 依赖图包含所有模块节点
- [ ] 端到端数据流已覆盖项目的核心业务场景（由代码中实际存在的路径自然决定数量）
- [ ] 每个数据流步骤描述的是跨模块交互（L1），非模块内部实现（L2）
- [ ] 全部外部依赖已列出，含故障影响说明
- [ ] 外部依赖表涵盖所有运行时依赖（版本/协议细节归入 02-决策记录.md）
- [ ] 认知边界地图三子表已填写（已知已知 / 已知未知 / 推断结论）
- [ ] 无 "..." 占位符
- [ ] **模块命名检查**：无模块名包含"与/和/及"连接不相关概念；如有连接词，是否满足三条件例外（共享接口定义 + 同库编译 + 编译期互锁）？
- [ ] **符号锚点核查**：所有代码证据是否使用了符号锚点格式（ClassName::methodName() 或 filename.cpp -> functionName()）？行号仅作为附加缓存，不作为独立证据引用
- [ ] 观察便签已写入 project-log.md
- [ ] **数据流深度检查**：列出系统所有主要用户操作，是否每个主要操作都有独立的数据流追踪？如有操作缺少追踪，在文档中说明原因
- [ ] **模块粒度检查**：是否存在一个"模块"包含了分属不同架构层级的类（如 View 和 Model 混在一个模块中）？如有，是否有明确的合并理由记录在概述中？
- [ ] **认知边界检查**：以下维度逐项确认是否真的全部已知。如某维度为空，确认原因并记录：架构决策理由 / 未实现功能的预期行为 / 外部依赖的内部实现 / 性能约束的来源

## 常见错误（反例对照）

### 错误1：数据流步骤混入模块内部实现
❌ 错误：DF1 步骤5 "创建GTS设备模板实例(GtsDeviceTemplate)"——这是模块内部的实例化操作
✅ 正确：DF1 步骤应该是跨模块粒度的，如"计算节点 → 模型数据整合：传递设备模板参数"

### 错误2：架构文档使用推测性措辞
❌ 错误："Agent推断该模块可能是用于..." "笔者推测这里的设计意图是..."
✅ 正确：所有结论以代码证据支撑。不确定的归入"认知边界地图→已知未知"，
    使用断言式语言（"该模块负责..."而非"该模块可能负责..."）

### 错误3：Mermaid图含外部库节点
❌ 错误：在模块依赖图中画出 Qt5::Widgets、SQLite 等外部库节点
✅ 正确：Mermaid图仅含项目内部模块，外部依赖在"外部依赖"表格中描述

---

## 确认清单

产出后暂停，输出以下确认清单：

```
📋 阶段 2 确认清单 — 架构梳理

请逐一检查以下内容，如有问题请直接说明（如"模块 X 不对，应该是 Y"或"A 和 B 应该合并"）：

□ 模块列表是否完整？有无遗漏？
□ 依赖关系图是否准确反映了实际的 import/include 关系？
□ 外部依赖是否完整？
□ 认知边界地图中的"已知未知"是否需要补充或修正？
□ 观察便签是否有遗漏的线索需要补充？

另外，如果有任何补充信息（比如某模块的历史背景、为什么做某个设计决策等），请直接告诉我，我会记录到文档中。
```

## 人类确认后的强制更新步骤

收到人类回复后，在执行分支处理之前，**必须**按顺序完成：

1. **逐条更新文档内容**：按人类回复修改文档（修改/追加/删除）
2. **更新 front matter**：`last_updated`（今天日期）、`last_code_verified`（今天日期）、`status`（`reviewed`）
3. **在 project-log.md 追加记录**：`[日期] 阶段2 经人类确认`

只有完成以上步骤后，才进入阶段 3。

## 人类回复后分支处理

| 人类反馈 | 你的操作 |
|---------|---------|
| 确认全部正确 | 执行上方「强制更新步骤」更新文档和 front matter |
| 某内容需要修改 | 按人类指示直接修改文档；修改后的内容直接更新；然后执行「强制更新步骤」 |
| 补充了新信息 | 追加到对应章节；然后执行「强制更新步骤」 |
| 有内容不确定/跳过 | 对应内容保持待确认标记；更新 front matter `last_updated` |
| 人类指出了遗漏 | 补充分析，补全文档，重新输出确认清单 |
| 人类未回复/跳过 | 内容保持不变；继续下一阶段 |

## 完成后更新 front matter

- `last_updated`: 今天日期
- `status`: `reviewed`（如果人类确认）或保持 `draft`（如果未回复）
- `last_code_verified`: 今天日期（如果人类确认）
