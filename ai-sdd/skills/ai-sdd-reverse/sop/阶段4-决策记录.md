# 阶段 4：决策记录

**目标**：技术栈盘点 + 从代码观察决策迹象 + Agent 必须给出推断理由（不能留空）。

> **本 SOP 的上下文**：
> - 文件角色定义 → `../SKILL.md`「2. 文件角色对照表」
> - 质量规则（正向/负向） → `../SKILL.md`「1. 通用规则」
> - 产出模板路径见 SKILL.md「各阶段资源映射」表
> - 全局约束：代码证据使用符号锚点格式（ClassName::methodName() 或 filename.cpp -> functionName()）

## 执行步骤

### 1. 技术栈分析

**分析依赖**：从包管理器文件中提取所有直接依赖项及其版本

**版本漂移评估**：每个关键依赖对比当前版本 vs 最新稳定版。
- 版本对比事实写入 `02-决策记录.md` > 版本漂移
- 风险判断写入 `04-问题与改进.md` > 版本与依赖风险

**构建/测试/部署验证**：从实际配置文件中提取工具链信息

**技术约束提取**：记录代码中隐含的技术禁止事项

### 2. 决策迹象观察

扫描代码，寻找可观察到的设计选择痕迹。分组记录：

**架构级决策**（影响多个模块或全系统）：
- 模块划分方式（如按功能/按层次/按领域）
- 通信方式选择（如 HTTP/消息队列/共享内存）
- 数据存储策略（如单库/多库、读写分离）
- 部署架构选择（如单体/微服务、容器化）
- 跨切面决策（如统一认证方式、日志策略、错误处理模型）

**设计级决策**（影响单个或少数模块）：
- 设计模式采用（如工厂/观察者/策略等，从代码结构识别）
- 数据结构选择（如为何用 map 而非 vector、为何自定义容器）
- 线程模型选择（如单线程事件循环/线程池/手动管理）
- 资源管理策略（如连接池大小、缓存策略、超时策略）

### 3. 推断理由（强制）

对每个观察到的决策迹象，Agent **必须**给出推断理由。格式：

```
Agent推断：[理由]。证据：[符号锚点]
```

状态标记为 `Inferred`。

**禁止**：对观察到的决策仅记录事实而不写推断理由。

### 4. 关于决策状态

决策记录使用以下 4 种状态：

| 状态 | 含义 | Agent 消费规则 |
|------|------|---------------|
| `Confirmed` | 人类确认过该决策及其理由 | 可作为后续分析的可靠依据 |
| `Inferred` | Agent 观察到了决策痕迹并给出了推断理由 | 可用于分析但应标注不确定性 |
| `Observed` | 观察到了决策痕迹但无法推断合理理由 | 需要人类补充理由 |
| `Untraceable` | 无法从代码追溯到任何相关依据 | 标记为无记录，依赖人类记忆 |

### 5. 写入观察便签

在 `.ai/project-log.md` 底部的「诊断观察便签」区段中追加「阶段4 观察」子节，仅记录 符号锚点 + 一句话描述：

示例：
```markdown
### 阶段4 观察
- [mainWindow_slot2.cpp -> g_globals] 7 个 g_ 前缀全局变量跨 4 个 .cpp 共享
- [scanmodel.h -> ScanModel::m_devDetailMap/m_pCompAry] 双重数据结构
- [sim_action_adaptor.cpp -> SimActionAdaptor::connect()] 混用旧式 SIGNAL()/SLOT() 宏（项目其余使用新式语法）
```

**便签规则**：只记录位置 + 事实，不评级、不分类。不确定的线索也写入，标注"? 需阶段6确认"。

### 6. 模式诊断清单

本阶段在分析代码时，逐项审视以下维度（发现后写入观察便签，不自行写入 04）：

- [ ] 全局可变状态检查：扫描 g_ 前缀变量、extern 声明、static 非 const 变量
  - 跨编译单元共享？是否有同步保护？-> 记录便签

- [ ] 编码约定一致性检查：扫描是否存在混用的语法/命名风格
  - 新旧 Qt connect 语法混用？中英文命名混用？-> 记录便签

- [ ] RAII 合规检查：扫描裸 new/delete
  - 无智能指针包装？手动 lock/unlock 无 RAII 封装？-> 记录便签

- [ ] 职责过载检查：
  - 单类方法数 >20 且涉及 >=3 种数据域？-> 记录便签
  - 单方法 >80 行且含 >=3 种操作？-> 记录便签

## 产出

- `.ai/doc/02-决策记录.md`（基于模板生成，含技术栈 + 决策记录）
- 观察便签 -> `.ai/project-log.md`（临时，阶段 6 消费）

## 产出后自检

**Agent 机械自检**（生成完成后立即逐项核对）：
- [ ] 所有直接依赖及版本已列出，含证据来源
- [ ] 构建/测试/部署配置已验证实际文件路径
- [ ] 版本漂移表已填写（仅版本对比事实）
- [ ] 决策迹象已按架构级和设计级分组记录
- [ ] 每条决策迹象包含 Agent 推断理由（不含空理由）
- [ ] 每条决策状态已标注（Confirmed / Inferred / Observed / Untraceable）
- [ ] 观察便签已写入 project-log.md
- [ ] 模式诊断清单（全局可变状态/编码约定/RAII/职责过载）已逐项检查，发现已记录便签
- [ ] **符号锚点核查**：所有代码证据是否使用了符号锚点格式（ClassName::methodName() 或 filename.cpp -> functionName()）？行号仅作为附加缓存，不作为独立证据引用

## 确认清单

产出后暂停，输出以下确认清单：

```
📋 阶段 4 确认清单 — 决策记录

□ 技术栈总览：所有主要技术是否都识别到了？有无遗漏或误判？
□ 关键依赖：版本号是否正确？
□ 版本漂移：需要特别关注的版本差距是否有遗漏？
□ 构建/测试/部署：工具链识别是否准确？
□ 决策记录：Agent 推断的理由是否合理？如有纠正请直接说明（如"选 XX 不是因为性能，而是因为团队熟悉"）
□ 是否有需要将 Inferred 状态升级为 Confirmed 的决策？
□ 观察便签是否有遗漏的线索需要补充？

如有纠正请直接说明（如"Redis 版本是 6.2 不是 7.0"、"漏了 Nginx"、"XX 决策理由不对，实际是..."）。
如需跳过此确认，回复"跳过"即可继续。
```

## 人类确认后的强制更新步骤

收到人类回复后，在执行分支处理之前，**必须**按顺序完成：

1. **升级决策状态**：人类确认了推断理由正确的 -> `Inferred` -> `Confirmed`；人类纠正了理由的 -> 更新理由并标记 `Confirmed`
2. **更新 front matter**：`last_updated`（今天日期）、`last_code_verified`（今天日期）、`status`（`reviewed`）
3. **在 project-log.md 追加记录**：`[日期] 阶段4 经人类确认，N 条决策升级为 Confirmed`

只有完成以上步骤后，才进入阶段 5。

## 人类回复后分支处理

| 人类反馈 | 你的操作 |
|---------|---------|
| 确认全部正确 / 回复"跳过" | 执行上方「强制更新步骤」后继续 |
| 某技术信息需要纠正 | 按人类指示直接修改文档；然后执行「强制更新步骤」 |
| 某推断理由需要修正 | 更新推断理由；标记为 Confirmed；然后执行「强制更新步骤」 |
| 补充了新决策或背景 | 追加到对应章节，标记为 Confirmed；然后执行「强制更新步骤」 |
| 人类将某 Inferred 升级 | 对应决策状态 -> Confirmed；然后执行「强制更新步骤」 |
| 人类未回复 | 所有内容保持原状态；继续下一阶段 |
