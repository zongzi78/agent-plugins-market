# 阶段 5：详细设计还原

**目标**：还原各模块的详细设计。

**核心原则**：深度与模块权重成正比——重要模块深入分析，简单模块如实简述。章节一个不落，深度自然有深有浅。

> **本 SOP 的上下文**：
> - 文件角色定义 → `../SKILL.md`「2. 文件角色对照表」
> - 质量规则（正向/负向） → `../SKILL.md`「1. 通用规则」
> - 产出模板路径见 SKILL.md「各阶段资源映射」表
> - 全局约束：代码证据使用符号锚点格式（ClassName::methodName() 或 filename.cpp -> functionName()）

## 综合权重评估规则

每个模块在分析前，Agent 必须评估其综合权重。权重决定分析的深度和篇幅比例：

**6 维度评估**：
| 维度 | 说明 |
|------|------|
| 代码量 | 模块的总行数 |
| 依赖数 | 被多少其他模块 import/include |
| 接口数 | 公开 API / public 方法数量 |
| 复杂度 | 圈复杂度、分支数、嵌套深度 |
| 修改频率 | 版本控制历史中的变更频率（如无 VCS 则跳过） |
| 业务中心性 | 是否处于核心业务流程的关键路径上 |

**权重判定**：
- 高权重模块（>=4 维突出）-> 深度分析，每个章节展开详述
- 中权重模块（2-3 维突出）-> 标准分析，关键章节详述、次要章节简述
- 低权重模块（<=1 维突出）-> 精简分析，以标准措辞为主，补充关键信息

**示例**：
- 一个 50 行、无依赖、无公开接口的工具类 -> 低权重 -> 主要为标准措辞
- 一个 2000 行、被 10+ 模块依赖的核心引擎 -> 高权重 -> 每章深度展开

## 深度与权重成正比规则

所有章节必须全部出现，但各章节的展开深度与模块权重成正比：
- 高权重模块：每章充分展开，含完整表格、Mermaid 图、详细分析
- 中权重模块：关键章（数据模型、接口契约、业务流程）展开，其余用标准措辞补充关键点
- 低权重模块：以标准措辞为主，仅在有实质内容时补 1-2 行高度总结

禁止以任何理由跳过章节。不涉及的章节使用标准措辞（见 `03-详细设计_模板.md` 的 `<!-- GEN: 章节规则 -->`），一字不改。

## God Class 必须单独成文

当遇到 God Class（单类 >=3 种职责，如 stage 2 观察便签中已记录的），该模块必须在概述中单独标注 "⚠️ God Class"，并对每一组职责分别建立独立的结构化说明（职责分组 -> 每组的字段/方法/数据流）。

## 模块划分内聚性约束（"与" test）

一个模块内的组件必须能通过 "与" test：如果将组件 A 和组件 B 放在同一个详细设计文档中，阅读者是否能同时理解两者的关系而无需交叉查阅其他文档？

- 通过 "与" test -> 可以放在同一文档（如 View + 其专用 ViewModel + 其专用 Data Model）
- 未通过 "与" test -> 必须拆分为独立文档（如 UserModel 和 NetworkConfig 无直接关系却放在同一模块）

Agent 在开始编写前，必须对每个模块执行 "与" test。

## 执行步骤

### 章节编写顺序（强制）

不要按模板的线性顺序写。按分析难度倒序，确保最高注意力用于最难分析的内容：
1. **先写**异常处理 + 并发安全（最需要深度分析，消耗最大）
2. **再写**核心业务流程 + 状态机
3. **再写**对外接口契约 + 核心数据模型（最容易从代码提取）
4. **最后写**概述（此时已对整个模块有深入理解，概述质量最高）

### 对每个模块执行

每个文件包含以下章节：

1. **概述**（必填）：
   - 核心职责的一句话精确定义
   - 在系统中的定位
   - 权重评估结果（如"高权重：被 12 个模块依赖，2200 行，核心业务引擎"）
   - God Class 标注（如适用，含职责分组概要）
   - 负向限制：如实列出模块明确不做什么

2. **核心数据模型**（必填）：
   - 列出核心字段
   - 实体间关系文字说明

3. **对外接口契约**（必填）：
   - 每个公开 API：签名、参数、返回值、副作用、幂等性

4. **核心业务流程**：
   - 如有 -> Mermaid 序列图，使用实际函数名/模块名
   - 图后简短摘要（2-3 行），不逐行重复图中信息
   - 如无 -> 标准措辞

5. **状态机**：如有 -> 状态表。如无 -> 标准措辞

6. **异常处理与容错**：如有 -> 异常处理表。如无 -> 标准措辞

7. **并发安全**：如有 -> 风险点表。如无 -> 标准措辞

8. **性能特征**：如有 -> 复杂度 + 资源密集点。如无 -> 标准措辞

9. **已知问题与陷阱**：-> 写入 `04-问题与改进.md` > 模块陷阱，不在各模块文档中重复

### 跨线程交互专项检查

在编写并发安全章节时，必须检查以下跨线程交互：

- [ ] 全局容器（g_ 前缀）的所有读写点 -> 标记所在线程
- [ ] QCoreApplication::postEvent 传递的数据 -> 检查发送线程和接收线程
- [ ] 信号槽跨线程连接（Qt::QueuedConnection）-> 检查传递的数据是否线程安全
- [ ] QMutexTryLocker 超时路径 -> 检查是否有回退/缓存/重试机制
- [ ] weak_ptr::lock() 调用点 -> 检查返回值是否判空

每个检查点如有风险 -> 记录为观察便签

### 多组件模块组织规则（强制）

如果一个详细设计文档覆盖 N >= 2 个独立类/组件：
- 核心数据模型 -> 每个组件必须有独立的字段表 + 约束
- 对外接口契约 -> 每个组件必须有独立的公开方法表
- 异常处理与容错 -> 每个组件必须有独立的异常场景表

**禁止**：在概述中列出某个组件但在后续章节中不再出现。

Agent 自检：概述中列出的每个组件，在数据模型、接口契约、异常处理三章中是否都有独立的子节？如有组件缺失 -> 补充分析或从概述中移除该组件。

### 写入观察便签

在 `.ai/project-log.md` 底部的「诊断观察便签」区段中追加「阶段5 观察」子节，仅记录 符号锚点 + 一句话描述：

示例：
```markdown
### 阶段5 观察
- [scanmodel.cpp -> ScanModel::updateDevDetailMap()] m_devDetailMap + m_pCompAry 双存储偏离风险
- [listmodel.cpp -> ListModel::append()] 被 CMsgThread 跨线程调用，内部无锁
- [scanmodel.cpp -> ScanModel::processScanResult()] QMutexTryLocker(500ms) 超时后消息静默丢失
- [simtaskstruct.h -> SimTask::pComp] weak_ptr，lock() 可能悬空
```

**便签规则**：只记录位置 + 事实，不评级、不分类。不确定的线索也写入，标注"? 需阶段6确认"。

## 产出

- `.ai/doc/03-详细设计/<模块中文名>.md`（每个模块一个文件）
  - 文件名优先使用中文（如"用户认证模块.md"），专有名词可用英文
- 观察便签 -> `.ai/project-log.md`（临时，阶段 6 消费）

> ⚠️ 不要删除 `.ai/doc/03-详细设计/_模板.md`。该文件是后续新增模块时的模板参照，必须保留。

## 产出后自检

每个模块文件生成后立即逐项核对：

**Agent 机械自检**：
- [ ] 概述含核心职责精确定义 + 权重评估结果
- [ ] God Class 已单独成文（如适用），含职责分组说明
- [ ] 模块划分 "与" test 已通过（如适用）
- [ ] 核心数据模型的关键字段已列出（深度与权重成正比）
- [ ] 全部公开 API 的契约完整（含副作用和幂等性）
- [ ] 所有章节全部出现（无遗漏）
- [ ] 不涉及的章节使用了标准措辞（无展开、无解释）
- [ ] 各章节深度与模块权重成正比（高权重充分展开，低权重精简）
- [ ] Mermaid 图后有简短摘要（非逐行重复）
- [ ] 观察便签已写入 project-log.md（跨线程交互、数据陷阱、哨兵值等线索）
- [ ] 跨线程交互专项检查已完成（5 个检查点）
- [ ] 多组件模块组织规则已遵守（如有 N>=2 组件，各章均有独立子节）
- [ ] 章节编写顺序已按推荐顺序执行（异常/并发 -> 流程/状态机 -> 接口/数据 -> 概述）
- [ ] **深度检查**：异常处理章节是否仅为标准 N/A 话术？如是，确认代码中确实无任何异常处理——检查每个 public 方法的 return 语句
- [ ] **深度检查**：并发安全章节是否仅为标准 N/A 话术？如是，确认模块确实无共享状态——检查所有成员变量的访问线程
- [ ] **符号锚点核查**：所有代码证据是否使用了符号锚点格式（ClassName::methodName() 或 filename.cpp -> functionName()）？行号仅作为附加缓存，不作为独立证据引用
- [ ] **权重-深度一致性检查**：对比模块的权重评估与各章节的展开深度，是否存在高权重模块的某些章节过于简略？或低权重模块的某些章节过于冗长？
- [ ] 文件名使用中文（取自模块的人类可理解名称）

语义质量项（设计描述准确性、状态标注合理性等）由确认清单覆盖。

## 确认清单

产出后暂停，输出以下确认清单：

```
📋 阶段 5 确认清单 — 详细设计还原

以下是各模块的设计描述摘要：
[每个模块列出：模块名、权重评估、API 数量、数据实体数量、关键设计点摘要]

请检查：
□ 各模块的权重评估是否合理？
□ 各模块的设计描述是否准确反映了代码实际？
□ 各模块的分析深度是否与权重匹配？
□ 观察便签是否有遗漏的线索需要补充？

如有问题请直接说明（如"用户模块的 API 描述不对，XX 接口实际上是做 YY 的"）。
```

## 人类确认后的强制更新步骤

收到人类回复后，在执行分支处理之前，**必须**按顺序完成：

1. **逐条更新各模块文档**：按人类回复修改各模块文件（修改/追加/删除）
2. **更新各模块 front matter**：`last_updated`（今天日期）、`last_code_verified`（今天日期）、`status`（`reviewed` 如果人类确认）
3. **在 project-log.md 追加记录**：`[日期] 阶段5 经人类确认`

只有完成以上步骤后，才进入阶段 6。

## 人类回复后分支处理

| 人类反馈 | 你的操作 |
|---------|---------|
| 确认全部正确 | 执行上方「强制更新步骤」更新各模块文档和 front matter |
| 某模块内容需要修改 | 按人类指示修改该模块文件；修改后内容直接更新；然后执行「强制更新步骤」 |
| 补充了新信息 | 追加到对应模块文件；然后执行「强制更新步骤」 |
| 有内容不确定 | 对应内容保持待确认标记；更新 `last_updated` |
| 人类未回复/跳过 | 内容保持不变；继续下一阶段 |
