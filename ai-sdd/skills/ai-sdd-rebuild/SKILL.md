---
name: ai-sdd-rebuild
description: >
  AI-SDD 文档腐烂修复。当 check 检测到文档漂移时，以腐烂的 AI-SDD 文档为 ref，
  调用 reverse 从代码重建文档。遵循 reverse 的全部质量规则和 SOP 标准。
  触发词：重建文档、重写规范、文档漂移修复、重建 docs、rebuild docs
---

# AI-SDD 文档腐烂修复

## 定位

当 `.ai/doc/` 规范文档与代码脱节（check 检测到漂移）时，本 skill 对腐烂的文档进行修复。
**rebuild 不是独立引擎**——它通过精心设计的提示词，调用 reverse 的 ref 模式来完成重建工作。

**核心逻辑**：以腐烂的 AI-SDD 文档为 ref（参考材料），以代码为真相源，执行"重组 + 修正 + 补全 + 保留"四步修复。

## 前置检查

1. 检查 `.ai/` 是否存在 → 如不存在，提示先运行 `/ai-sdd-init`
2. 检查 `.ai/doc/` 是否存在且有内容 → 如为空，提示"无文档可重建，建议运行 /ai-sdd-reverse 进行首次逆向化"

## 执行流程

### 步骤 1：运行漂移检测

按照 `/ai-sdd-check` 的流程，执行完整的漂移检测。产出诊断报告，包含：
- 哪些文件有漂移、漂移类型、严重程度
- 角色越界（内容出现在错误的文档中）
- 结构退化（追加式更新、内容堆在末尾）

### 步骤 2：展示诊断结果 + 确认重建范围

展示诊断报告摘要，列出建议重建的文件范围，用 AskUserQuestion 让人类确认：

```
=== 文档腐烂诊断摘要 ===

[漂移项列表]

建议重建范围：
  A) 全量重建 — 所有 doc 文件重新从代码生成
  B) 选择性重建 — 仅重建有漂移的文件：[列出]
  C) 取消
```

### 步骤 3：提取需保留的元信息

在重建前，读取旧文档并提取以下元信息：

- `02-决策记录.md` 中 `status=Confirmed` 的决策条目（人类确认过的决策理由）
- `01-行为目录.md` 中人类确认的行为状态裁决
- 各文档 front matter 中的 `last_code_verified` 等时间戳

这些元信息将作为"保留清单"传入 reverse，确保人类的知识积累不丢失。

### 步骤 4：调用 reverse ref 模式

将 `.ai/doc/` 作为 ref 路径传入，注入以下提示词，调用 reverse 执行重建：

```
## 场景：AI-SDD 文档腐烂修复

你正在为一个已接入 AI-SDD 的项目修复腐烂的规范文档。
项目当前 AI-SDD 版本：[从 .ai/supplement-rules.md 读取 version]

### 背景
该项目的 `.ai/doc/` 文档是通过 AI-SDD reverse 生成的。自生成以来，
代码持续演进但文档未同步更新，导致以下问题（详见 check 诊断报告）：

[此处插入步骤 1 的诊断报告摘要]

### Ref 文档的性质

你要处理的 ref 文档（`.ai/doc/` 下的所有文件）**不是**首次 reverse 时的
"外部参考文档"。它们是：

- **腐烂的 AI-SDD 文档**：内容可能过时、与代码事实不符
- **角色污染的材料**：某些内容出现在了错误的文档中
  （如 00-架构.md 中出现了 API 细节，01-行为目录.md 中出现了实现细节）
- **结构退化的文件**：存在追加式更新（内容堆在末尾）、组织混乱
- **有保留价值的碎片**：其中人类明确确认过的元信息需要保留

### 你的任务（按优先级排序）

#### 任务 1：重组（Reorganize）
逐份阅读 ref 文档，判断每条内容当前是否在其**正确的文档**中。
判断标准：各模板顶部的「📋 本文档角色」区块中的"应该写"/"不应该写"列表。

具体步骤：
1. 逐文档、逐章节阅读 ref 文档
2. 对每条内容判断：这段内容属于哪个文档的角色范围？
3. 内容在正确的文档 + 正确的章节 → 保留原位
4. 内容在错误的文档中 → 移动到正确文档的正确章节
5. 内容属于多个文档都不该有的（如实现过程记录、讨论记录）→ 删除

重组后，每份文档只包含属于它角色范围的内容。
不需要的内容不保留、不挪到末尾、不创建"杂项"章节。

#### 任务 2：修正（Correct）
逐条对照 check 诊断报告中的漂移项：
- 架构漂移 → 更新 `00-架构.md` 的模块列表、依赖关系（以代码实际目录结构为准）
- 行为漂移 → 更新 `01-行为目录.md` 的行为清单（新增/废弃，以代码实际 API/UI 入口为准）
- 设计漂移 → 更新 `03-详细设计/` 的 API 契约（以代码实际签名为准）
- 角色越界 → 已在任务 1 中处理

修正时的铁律：**以代码为准**。不因为 ref 文档说 X 就保留 X——
如果代码实际做的是 Y，文档必须写 Y。

#### 任务 3：补全（Complete）
像首次 reverse 一样遍历代码，发现 ref 文档中**完全没有覆盖**的内容：
- `00-架构.md`：代码中有但文档中没有的模块 → 按阶段 2 SOP 生成
- `01-行为目录.md`：代码中有但文档中没有的可观察行为 → 按阶段 3 SOP 生成
- `02-决策记录.md`：代码中可观察到的但文档中没有记录的技术栈事实和设计决策 → 按阶段 4 SOP 生成
- `03-详细设计/`：代码中有但文档中没有的模块详细设计 → 按阶段 5 SOP 生成
- `04-问题与改进.md`：从代码中新发现的问题 → 调用 ai-sdd-diagnose 生成

对新发现的内容，按 reverse 各阶段 SOP 的标准生成并填充到正确的文档中。

#### 任务 4：保留（Preserve）
以下内容在修正和重组时必须保留（除非代码事实已经改变）：

[此处插入步骤 3 提取的元信息清单]

保留规则：
- `02-决策记录.md` 中 `status=Confirmed` 的条目：保留理由文本。
  但如果代码事实已经改变（如原来用 QHash 现在改用 std::map），
  则更新代码事实部分，保留决策理由，标记为 `⚠️ 待重新确认`
- `01-行为目录.md` 中人类确认的行为状态裁决：保留裁决。
  但如果代码行为已经改变（如原来空函数体现在有了实现），
  则更新代码证据部分，保留裁决依据，标记为 `⚠️ 待重新确认`
- `last_code_verified` 等时间戳：保留原有时间戳，追加本次 rebuild 时间

### 与首次 reverse 的关键区别

| 维度 | 首次 reverse | 本次 rebuild |
|------|-------------|-------------|
| 起点 | 空白项目，无从参考 | 有腐烂的文档作为"待修复材料" |
| 阶段 0-1 | 需要（VC 探测、项目用途确认） | **跳过**——项目信息已在 project-log.md 中 |
| 对 ref 文档的态度 | 理解辅助，看完就放一边 | **待修复的主体**——需要逐条判断保留/移动/丢弃 |
| 核心动作 | 生成 | **重组 + 修正 + 补全 + 保留** |
| 元信息 | 无历史负担 | **必须保留人类确认过的内容** |

### 质量要求

遵循 reverse 的全部质量规则：
- **正向 6 条**：以真实代码为准、创建心态、尊重文件角色、结构化表述、标注符号锚点、提供增量价值
- **负向 5 条**：禁止"..."占位符、禁止大段原始代码、禁止跳过不确定的内容、禁止将推断包装为事实、禁止猜测业务含义

各阶段 SOP 的产出标准同样适用于对应的任务。
```

### 步骤 5：输出重建摘要

重建完成后，输出摘要：

```
✅ 文档重建完成。

重建范围：[全量 / 选择性]
重组：[N] 处内容移动到正确文档
修正：[N] 处过时描述已更新
补全：[N] 个新模块/[N] 个新行为/[N] 条新决策
保留：[N] 条人类确认的元信息已保留

⚠️ 待人类处理：
  - [如有标注"待确认"或"待重新确认"的内容，列出]
```
